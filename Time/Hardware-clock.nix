{ config, ... }: {

	# If true, keep the hardware clock in local time instead of UTC.
	# https://search.nixos.org/options?channel=24.05&show=time.hardwareClockInLocalTime
	time.hardwareClockInLocalTime = true;

}
